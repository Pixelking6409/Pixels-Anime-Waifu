module.exports = {
    name: "prefix",
    description: "Sets new server prefix for the bot",
    usage: "<prefix>",
    aliases: ["commands"],
    type: "utility",

    execute(message) {
        
    }
}